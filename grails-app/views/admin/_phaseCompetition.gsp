<div class="phase_${idPhase}">
    <fieldset>
        <legend><input type="text" name="descrizioneFase" /></legend>
        <a href="javascript:void(0)" onclick="addRound(${idPhase})">Aggiungi turno</a>
        <div class="rounds_${idPhase}">
        </div>
    </fieldset>
</div>